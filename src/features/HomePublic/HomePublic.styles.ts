import { StyleSheet } from "react-native";
import { sizes } from "src/shared/lib/constants";

export const styles = StyleSheet.create({
  container: {
    marginTop: sizes.small,
    alignItems: "center",
  },
});
